Sources for the plugin `plugin.onePassword`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.onePassword" = {
            publisherId = "tech.scotth",
        },
    },
}
```
